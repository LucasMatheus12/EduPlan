# Com base = "eduPlan", o publish é relativo a essa pasta → "out" = eduPlan/out na raiz do repo.
# No painel Netlify: Publish directory vazio ou "out" — NÃO use "./" nem a raiz do repo.

[build]
  base = "./"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
